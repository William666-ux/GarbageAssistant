# =========================
# 智能垃圾分类助手 requirements.txt
# 基于当前 garbage 环境整理
# 适用于当前 main.py：
# Streamlit 页面 + DeepSeek RAG + 本地 HuggingFace Embedding + Chroma + YOLO 图片识别
# Python 建议版本：3.10
# =========================

# ---------- 基础科学计算 ----------
# 当前 garbage 环境使用 numpy 2.x + pandas 2.3.x，可以正常配合新版 Streamlit / LangChain 使用
numpy==2.2.6
pandas==2.3.3
pillow==10.4.0
matplotlib==3.10.9
tqdm==4.68.1
scikit-learn==1.7.2

# ---------- Streamlit 应用 ----------
streamlit==1.29.0

# ---------- LangChain / DeepSeek / RAG ----------
# 当前 main.py 使用：
# from langchain_openai import ChatOpenAI
# from langchain_community.embeddings import HuggingFaceEmbeddings
# from langchain_community.vectorstores import Chroma
# from langchain_community.document_loaders import DirectoryLoader, UnstructuredWordDocumentLoader
langchain==0.2.17
langchain-community==0.2.19
langchain-core==0.2.43
langchain-openai==0.1.25
langchain-text-splitters==0.2.4
openai==1.55.3
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4

# ---------- Chroma 向量数据库 ----------
chromadb==1.5.9
tiktoken==0.13.0

# ---------- Word 文档知识库读取 ----------
unstructured==0.18.32
unstructured-client==0.42.12
python-docx==1.2.0
pypdf==6.13.0
pypdfium2==5.9.0
python-magic==0.4.27
python-oxmsg==0.0.2
beautifulsoup4==4.15.0
lxml==6.1.1

# ---------- 本地 Embedding ----------
# main.py 使用：BAAI/bge-small-zh-v1.5
sentence-transformers==5.5.1
transformers==5.10.2
huggingface_hub==1.18.0
tokenizers==0.22.2
safetensors==0.7.0

# ---------- YOLO 垃圾图片识别 ----------
ultralytics==8.4.61
ultralytics-thop==2.0.20
opencv-python==4.13.0.92

# ---------- PyTorch ----------
# 用于 YOLO 推理和 sentence-transformers
# 注意：如果 Windows 本地 torch 2.12.0 出现 DLL 或兼容问题，
# 可改用官方 CPU 版单独安装：
# pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
torchvision==0.27.0
